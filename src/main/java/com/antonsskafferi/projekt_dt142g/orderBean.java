package com.antonsskafferi.projekt_dt142g;

import java.util.List;

public class orderBean {

    private List<OrderItem> itemsList;

    //TODO: implement a method for pushing an item to itemsList 👍👍👍🌲
    //TODO: if the item already exists, increment OrderItem.amount :-) 🫡
/*
    public void push(int amount, String name){
        OrderItem item = new OrderItem(amount, name);
        if(!itemsList.contains(item)) {
            itemsList.add(item);
        }
        else{
            //find the index of existing item (based on string!!!)
            //change that item's
        }
    }
    */

    /*
      This bean will contain all meals and drinks that will go to a particular table.
      Each unique meal or drink will be stored as a unique class called "OrderItem"

      class OrderItem contains the following info:
       -    int amount      // the amount of this item that will be ordered
       - String name        // the name of the item
     */
}
